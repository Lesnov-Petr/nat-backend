const {
  getContacts,
  getContactByID,
  addContactNew,
  deleteContactByID,
} = require("./controllerContacts");

module.exports = {
  getContacts,
  getContactByID,
  addContactNew,
  deleteContactByID,
};
